var re = Object.defineProperty;
var ne = (e, r, t) => r in e ? re(e, r, { enumerable: !0, configurable: !0, writable: !0, value: t }) : e[r] = t;
var P = (e, r, t) => ne(e, typeof r != "symbol" ? r + "" : r, t);
var K = /* @__PURE__ */ ((e) => (e.none = "none", e.create = "create", e.login = "login", e))(K || {}), H = /* @__PURE__ */ ((e) => (e.organizationDetails = "organization_details", e.organizationMembers = "organization_members", e.organizationPlanDetails = "organization_plan_details", e.organizationPaymentDetails = "organization_payment_details", e.organizationPlanSelection = "organization_plan_selection", e.paymentDetails = "payment_details", e.planSelection = "plan_selection", e.planDetails = "plan_details", e.profile = "profile", e))(H || {}), L = /* @__PURE__ */ ((e) => (e.logout = "logout", e.login = "login", e.register = "registration", e.token = "token", e.profile = "profile", e))(L || {}), y = /* @__PURE__ */ ((e) => (e[e.refreshToken = 0] = "refreshToken", e[e.cookie = 1] = "cookie", e))(y || {});
const F = (e) => {
  const r = (o) => btoa(o).replace(/\+/g, "-").replace(/\//g, "_").replace(/=+$/, "");
  if (e instanceof ArrayBuffer) {
    const o = new Uint8Array(e), s = String.fromCharCode(...o);
    return r(s);
  }
  const t = new TextEncoder().encode(e), n = String.fromCharCode(...t);
  return r(n);
}, M = (e = 28) => {
  if (crypto) {
    const r = new Uint8Array(e / 2);
    return crypto.getRandomValues(r), Array.from(r, oe).join("");
  } else
    return se(e);
};
function oe(e) {
  return e.toString(16).padStart(2, "0");
}
function se(e = 28) {
  const r = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789";
  let t = "";
  const n = r.length;
  for (let o = 0; o < e; o++)
    t += r.charAt(Math.floor(Math.random() * n));
  return t;
}
const j = (e) => {
  let r = e.replace(/([^:]\/)\/+/g, "$1");
  return r.match(/^\/\/[^/?]+/) && r.includes(".") ? r = r.replace(/^\/\/\/+/, "//") : r.match(/^\/\/+/) && (r = r.replace(/^\/\/+/, "/")), r !== "/" && (r = r.replace(/\/$/, "")), r;
}, ie = (e, r = !1) => {
  var o, s, l, d;
  const t = Array.isArray(e.audience) ? e.audience.join(" ") : e.audience || "", n = {
    login_hint: e.loginHint,
    is_create_org: (o = e.isCreateOrg) == null ? void 0 : o.toString(),
    connection_id: e.connectionId,
    redirect_uri: e.redirectURL ? r ? e.redirectURL : j(e.redirectURL) : void 0,
    audience: t,
    scope: ((s = e.scope) == null ? void 0 : s.join(" ")) || "email profile openid offline",
    prompt: e.prompt,
    lang: e.lang,
    org_code: e.orgCode,
    org_name: e.orgName,
    has_success_page: (l = e.hasSuccessPage) == null ? void 0 : l.toString(),
    workflow_deployment_id: e.workflowDeploymentId,
    supports_reauth: (d = e.supportsReauth) == null ? void 0 : d.toString(),
    plan_interest: e.planInterest,
    pricing_table_key: e.pricingTableKey,
    pages_mode: e.pagesMode
  };
  return Object.keys(n).forEach(
    (u) => n[u] === void 0 && delete n[u]
  ), n;
}, N = (e) => typeof e != "object" || e === null ? e : Array.isArray(e) ? e.map((r) => N(r)) : Object.fromEntries(
  Object.entries(e).map(([r, t]) => [
    r.replace(/_([a-z])/g, (n, o) => o.toUpperCase()),
    N(t)
  ])
), ae = [
  // UTM tags
  "utm_source",
  "utm_medium",
  "utm_campaign",
  "utm_content",
  "utm_term",
  // Google Ads smart campaign tracking
  "gclid",
  "click_id",
  "hsa_acc",
  "hsa_cam",
  "hsa_grp",
  "hsa_ad",
  "hsa_src",
  "hsa_tgt",
  "hsa_kw",
  "hsa_mt",
  "hsa_net",
  "hsa_ver",
  // Marketing category
  "match_type",
  "keyword",
  "device",
  "ad_group_id",
  "campaign_id",
  "creative",
  "network",
  "ad_position",
  "fbclid",
  "li_fat_id",
  "msclkid",
  "twclid",
  "ttclid"
], ye = async (e, r = L.login, t, n) => {
  const o = `${e}/oauth2/auth`, s = $();
  if (t.reauthState)
    try {
      const a = N(
        JSON.parse(atob(t.reauthState))
      );
      t = {
        ...t,
        ...a
      }, delete t.reauthState;
    } catch (a) {
      const h = a instanceof Error ? a.message : "Unknown error";
      throw new Error(`Error handing reauth state: ${h}`);
    }
  if (!t.clientId)
    throw new Error("Error generating auth URL: Client ID missing");
  const l = {
    client_id: t.clientId,
    response_type: t.responseType || "code",
    ...ie(t, n == null ? void 0 : n.disableUrlSanitization)
  };
  t.state || (t.state = M(32)), s && s.setSessionItem(c.state, t.state), l.state = t.state, t.nonce || (t.nonce = M(16)), l.nonce = t.nonce, s && s.setSessionItem(c.nonce, t.nonce);
  let d = "";
  if (t.codeChallenge)
    l.code_challenge = t.codeChallenge;
  else {
    const { codeVerifier: a, codeChallenge: h } = await ce();
    d = a, s && s.setSessionItem(c.codeVerifier, a), l.code_challenge = h;
  }
  l.code_challenge_method = "S256", t.codeChallengeMethod && (l.code_challenge_method = t.codeChallengeMethod), !t.prompt && r === L.register && (l.prompt = K.create), t.properties && Object.keys(t.properties).forEach((a) => {
    var f;
    if (!ae.includes(a)) {
      console.warn("Unsupported Property for url generation: ", a);
      return;
    }
    const h = (f = t.properties) == null ? void 0 : f[a];
    h !== void 0 && (l[a] = h);
  });
  const u = new URLSearchParams(l).toString();
  return {
    url: new URL(`${o}?${u}`),
    state: l.state,
    nonce: l.nonce,
    codeChallenge: l.code_challenge,
    codeVerifier: d
  };
};
async function ce() {
  const e = M(52), r = new TextEncoder().encode(e);
  let t = "";
  if (!crypto)
    t = F(btoa(e));
  else {
    const n = await crypto.subtle.digest("SHA-256", r);
    t = F(n);
  }
  return { codeVerifier: e, codeChallenge: t };
}
const R = () => typeof window < "u";
let b;
function q(e, r) {
  if (O(), !R())
    throw new Error("setRefreshTimer requires a browser environment");
  if (e <= 0)
    throw new Error("Timer duration must be positive");
  b = window.setTimeout(
    r,
    Math.min(e * 1e3 - 1e4, 864e5)
  );
}
function O() {
  R() && b !== void 0 && (window.clearTimeout(b), b = void 0);
}
const I = {
  framework: "",
  frameworkVersion: "",
  sdkVersion: ""
}, W = async () => {
  var e;
  await ((e = $()) == null ? void 0 : e.removeItems(
    c.state,
    c.nonce,
    c.codeVerifier
  ));
}, le = () => `${I.framework}/${I.sdkVersion}/${I.frameworkVersion}/Javascript`, ke = async ({
  urlParams: e,
  domain: r,
  clientId: t,
  clientSecret: n,
  redirectURL: o,
  autoRefresh: s = !1,
  onRefresh: l
}) => {
  const d = e.get("state"), u = e.get("code");
  if (!d || !u)
    return console.error("Invalid state or code"), {
      success: !1,
      error: "Invalid state or code"
    };
  const a = $();
  if (!a)
    return console.error("No active storage found"), {
      success: !1,
      error: "Authentication storage is not initialized"
    };
  (!I.framework || !I.frameworkVersion) && console.warn(
    "Framework and version not set. Please set the framework and version in the config object"
  );
  const h = await a.getSessionItem(c.state);
  if (d !== h)
    return console.error("Invalid state"), {
      success: !1,
      error: `Invalid state; supplied ${d}, expected ${h}`
    };
  const f = await a.getSessionItem(
    c.codeVerifier
  );
  if (f === null)
    return console.error("Code verifier not found"), {
      success: !1,
      error: "Code verifier not found"
    };
  const m = {
    "Content-type": "application/x-www-form-urlencoded; charset=UTF-8"
  };
  I.framework && (m["Kinde-SDK"] = le());
  const S = !i.useInsecureForRefreshToken && E(r) ? {
    credentials: "include"
  } : {}, v = new URLSearchParams({
    client_id: t,
    code: u,
    code_verifier: f,
    grant_type: "authorization_code",
    redirect_uri: o
  });
  n && v.append("client_secret", n);
  const ee = {
    method: "POST",
    ...S,
    headers: new Headers(m),
    body: v
  };
  let w;
  O();
  try {
    if (w = await fetch(`${r}/oauth2/token`, ee), !(w != null && w.ok)) {
      const p = await w.text();
      return console.error("Token exchange failed:", w.status, p), {
        success: !1,
        error: `Token exchange failed: ${w.status} - ${p}`
      };
    }
  } catch (p) {
    return W(), console.error("Token exchange failed:", p), {
      success: !1,
      error: `Token exchange failed: ${p}`
    };
  }
  const g = await w.json(), D = T();
  D && D.setItems({
    [c.accessToken]: g.access_token,
    [c.idToken]: g.id_token,
    [c.refreshToken]: g.refresh_token
  }), (i.useInsecureForRefreshToken || !E(r)) && a.setSessionItem(c.refreshToken, g.refresh_token), s && q(g.expires_in, async () => {
    x({ domain: r, clientId: t, onRefresh: l });
  }), W();
  const te = (p) => (p.search = "", p);
  if (R()) {
    const p = te(new URL(window.location.toString()));
    window.history.replaceState(window.history.state, "", p);
  }
  return !g.access_token || !g.id_token || !g.refresh_token ? {
    success: !1,
    error: "No access token received"
  } : {
    success: !0,
    [c.accessToken]: g.access_token,
    [c.idToken]: g.id_token,
    [c.refreshToken]: g.refresh_token
  };
};
function ue(e) {
  const r = document.cookie.split("; ").find((t) => t.startsWith(`${e}=`));
  if (!r) return null;
  try {
    const t = r.split("=")[1];
    return t ? decodeURIComponent(t) : null;
  } catch (t) {
    return console.error(`Error parsing cookie ${e}:`, t), null;
  }
}
const de = "_kbrte", Te = async ({
  domain: e,
  clientId: r
}) => {
  if (!e)
    return {
      success: !1,
      error: "Domain is required for authentication check"
    };
  if (!r)
    return {
      success: !1,
      error: "Client ID is required for authentication check"
    };
  const t = T();
  if (t) {
    const {
      [c.accessToken]: l,
      [c.idToken]: d,
      [c.refreshToken]: u
    } = await t.getItems(
      c.accessToken,
      c.idToken,
      c.refreshToken
    );
    if (l && d && u)
      return await X({ threshold: 10 }) ? await x({
        domain: e,
        clientId: r,
        refreshType: y.refreshToken
      }) : {
        success: !0,
        accessToken: l,
        idToken: d,
        refreshToken: u
      };
  }
  const n = E(e), o = i.useInsecureForRefreshToken;
  let s = null;
  return n && !o && (s = ue(de)), await x({
    domain: e,
    clientId: r,
    refreshType: s ? y.cookie : y.refreshToken
  });
}, E = (e) => !e.match(
  /^(?:https?:\/\/)?[a-zA-Z0-9][.-a-zA-Z0-9]*\.kinde\.com$/i
);
function B(e, r) {
  return r <= 0 ? [] : e.match(new RegExp(`.{1,${r}}`, "g")) || [];
}
var c = /* @__PURE__ */ ((e) => (e.accessToken = "accessToken", e.idToken = "idToken", e.refreshToken = "refreshToken", e.state = "state", e.nonce = "nonce", e.codeVerifier = "codeVerifier", e))(c || {}), z = /* @__PURE__ */ ((e) => (e.preWarning = "preWarning", e.timeout = "timeout", e))(z || {});
class G {
  constructor() {
    P(this, "listeners", /* @__PURE__ */ new Set());
    P(this, "notificationScheduled", !1);
  }
  notifyListeners() {
    this.listeners.size !== 0 && this.scheduleNotification();
  }
  async scheduleNotification() {
    this.notificationScheduled || (this.notificationScheduled = !0, await new Promise((r) => {
      setTimeout(async () => {
        await Promise.all(
          Array.from(this.listeners).map((t) => t())
        ), this.notificationScheduled = !1, r();
      }, 0);
    }));
  }
  subscribe(r) {
    return this.listeners.add(r), () => {
      this.listeners.delete(r);
    };
  }
  async setItems(r) {
    await Promise.all(
      Object.entries(r).map(
        ([t, n]) => this.setSessionItem(t, n)
      )
    );
  }
  async getItems(...r) {
    const t = r.map(async (o) => {
      const s = await this.getSessionItem(o);
      return [o, s];
    }), n = await Promise.all(t);
    return Object.fromEntries(n);
  }
  async removeItems(...r) {
    await Promise.all(
      r.map((t) => this.removeSessionItem(t))
    );
  }
}
class _e extends G {
  constructor() {
    super(...arguments);
    P(this, "memCache", {});
  }
  /**
   * Clears all items from session store.
   * @returns {void}
   */
  async destroySession() {
    this.memCache = {}, this.notifyListeners();
  }
  /**
   * Sets the provided key-value store to the memory cache.
   * @param {string} itemKey
   * @param {unknown} itemValue
   * @returns {void}
   */
  async setSessionItem(t, n) {
    if (await this.removeSessionItem(t), typeof n == "string") {
      B(n, i.maxLength).forEach(
        (o, s) => {
          this.memCache[`${i.keyPrefix}${t}${s}`] = o;
        }
      ), this.notifyListeners();
      return;
    }
    this.memCache[`${i.keyPrefix}${String(t)}0`] = n, this.notifyListeners();
  }
  /**
   * Gets the item for the provided key from the memory cache.
   * @param {string} itemKey
   * @returns {unknown | null}
   */
  async getSessionItem(t) {
    if (this.memCache[`${i.keyPrefix}${String(t)}0`] === void 0)
      return null;
    let n = "", o = 0, s = `${i.keyPrefix}${String(t)}${o}`;
    for (; this.memCache[s] !== void 0; )
      n += this.memCache[s], o++, s = `${i.keyPrefix}${String(t)}${o}`;
    return n;
  }
  /**
   * Removes the item for the provided key from the memory cache.
   * @param {string} itemKey
   * @returns {void}
   */
  async removeSessionItem(t) {
    for (const n in this.memCache)
      n.startsWith(`${i.keyPrefix}${String(t)}`) && delete this.memCache[n];
    this.notifyListeners();
  }
}
class Se extends G {
  constructor() {
    super();
    P(this, "internalItems", /* @__PURE__ */ new Set());
    i.useInsecureForRefreshToken && console.warn("LocalStorage store should not be used in production");
  }
  /**
   * Clears all items from session store.
   * @returns {void}
   */
  async destroySession() {
    await Promise.all(
      Array.from(this.internalItems).map((t) => this.removeSessionItem(t))
    ), this.notifyListeners();
  }
  /**
   * Sets the provided key-value store to the localStorage cache.
   * @param {V} itemKey
   * @param {unknown} itemValue
   * @returns {void}
   */
  async setSessionItem(t, n) {
    if (await this.removeSessionItem(t), this.internalItems.add(t), typeof n == "string") {
      B(n, i.maxLength).forEach(
        (o, s) => {
          localStorage.setItem(
            `${i.keyPrefix}${t}${s}`,
            o
          );
        }
      ), this.notifyListeners();
      return;
    }
    localStorage.setItem(
      `${i.keyPrefix}${t}0`,
      n
    ), this.notifyListeners();
  }
  /**
   * Gets the item for the provided key from the localStorage cache.
   * @param {string} itemKey
   * @returns {unknown | null}
   */
  async getSessionItem(t) {
    if (localStorage.getItem(`${i.keyPrefix}${t}0`) === null)
      return null;
    let n = "", o = 0, s = `${i.keyPrefix}${String(t)}${o}`;
    for (; localStorage.getItem(s) !== null; )
      n += localStorage.getItem(s), o++, s = `${i.keyPrefix}${String(t)}${o}`;
    return n;
  }
  /**
   * Removes the item for the provided key from the localStorage cache.
   * @param {V} itemKey
   * @returns {void}
   */
  async removeSessionItem(t) {
    let n = 0;
    for (; localStorage.getItem(
      `${i.keyPrefix}${String(t)}${n}`
    ) !== null; )
      localStorage.removeItem(
        `${i.keyPrefix}${String(t)}${n}`
      ), n++;
    this.internalItems.delete(t), this.notifyListeners();
  }
}
const i = {
  /**
   * The prefix to use for the storage keys.
   */
  keyPrefix: "kinde-",
  /**
   * The maximum length of the storage.
   *
   * If the length is exceeded the items will be split into multiple storage items.
   */
  maxLength: 2e3,
  /**
   * Use insecure storage for refresh token.
   *
   * Warning: This should only be used when you're not using a custom domain and no backend app to authenticate on.
   */
  useInsecureForRefreshToken: !1,
  /**
   * The number of minutes of inactivity before tokens are considered expired.
   *
   * When undefined, activity tracking is disabled.
   */
  activityTimeoutMinutes: void 0,
  /**
   * The number of minutes of inactivity before a pre-warning is shown.
   *
   * When undefined, pre-warning is disabled.
   */
  activityTimeoutPreWarningMinutes: void 0,
  /**
   * The function to call when the activity timeout is reached.
   *
   * @param timeoutType - The type of timeout that occurred.
   */
  onActivityTimeout: void 0
};
let C = null, U = null;
const fe = () => {
  const e = T() ?? $();
  if (!e)
    throw new Error("Session manager not found");
  if (!i.activityTimeoutMinutes)
    throw new Error("No activity timeout minutes set");
  if (i.activityTimeoutPreWarningMinutes !== void 0 && i.activityTimeoutPreWarningMinutes >= i.activityTimeoutMinutes)
    throw new Error(
      "activityTimeoutPreWarningMinutes must be less than activityTimeoutMinutes"
    );
  C && clearTimeout(C), U && clearTimeout(U), U = setTimeout(
    async () => {
      var t;
      try {
        await e.destroySession();
      } catch (n) {
        console.error("Failed to destroy secure session:", n);
      }
      const r = $();
      if (r && r !== e)
        try {
          await r.destroySession();
        } catch (n) {
          console.error("Failed to destroy insecure session:", n);
        }
      try {
        (t = i.onActivityTimeout) == null || t.call(i, z.timeout);
      } catch (n) {
        console.error(
          "[activityTimeout] onActivityTimeout(timeout) threw:",
          n
        );
      }
    },
    i.activityTimeoutMinutes * 60 * 1e3
  ), i.activityTimeoutPreWarningMinutes !== void 0 && (C = setTimeout(
    () => {
      var r;
      try {
        (r = i.onActivityTimeout) == null || r.call(i, z.preWarning);
      } catch (t) {
        console.error(
          "[activityTimeout] onActivityTimeout(preWarning) threw:",
          t
        );
      }
    },
    i.activityTimeoutPreWarningMinutes * 60 * 1e3
  ));
}, J = (e) => {
  if (!e)
    throw new Error("Session manager not found");
  if (!i.activityTimeoutMinutes)
    return e;
  if (i.activityTimeoutPreWarningMinutes !== void 0 && i.activityTimeoutPreWarningMinutes >= i.activityTimeoutMinutes)
    throw new Error(
      "activityTimeoutPreWarningMinutes must be less than activityTimeoutMinutes"
    );
  const r = {
    get(t, n) {
      const o = t[n];
      return typeof o == "function" ? (n !== "destroySession" && fe(), o.bind(t)) : o;
    }
  };
  return new Proxy(e, r);
}, Z = (e, r) => {
  if (Array.isArray(e) && Array.isArray(r))
    return Array.from(/* @__PURE__ */ new Set([...e, ...r]));
  if (e && typeof e == "object" && r && typeof r == "object") {
    const t = { ...e };
    for (const n of Object.keys(r))
      n in t ? t[n] = Z(t[n], r[n]) : t[n] = r[n];
    return t;
  }
  return r;
};
async function V(e) {
  const r = T();
  if (!r)
    throw new Error("No active storage found.");
  const t = await r.getSessionItem(c.accessToken);
  if (!t)
    throw new Error("Authentication token not found.");
  const n = await A("iss");
  if (!(n != null && n.value))
    throw new Error("Domain (iss claim) not found.");
  let o;
  try {
    o = await fetch(`${n.value}/${e}`, {
      method: "GET",
      headers: {
        Authorization: `Bearer ${t}`,
        "Content-Type": "application/json"
      }
    });
  } catch (s) {
    throw new Error(`Failed to fetch from ${n.value}/${e}: ${s}`);
  }
  if (!o.ok)
    throw new Error(`API request failed with status ${o.status}`);
  return await o.json();
}
const Y = async ({
  url: e
}) => {
  var n;
  let r = [], t = await V(e);
  if (r = t.data, (n = t.metadata) == null ? void 0 : n.has_more) {
    let o = t.metadata.next_page_starting_after;
    for (; t.metadata.has_more; )
      t = await V(
        `${e}?starting_after=${o}`
      ), r = Z(r, t.data), o = t.metadata.next_page_starting_after;
  }
  return r;
}, ve = async (e) => {
  const r = await _();
  if (!r)
    return {
      orgCode: null,
      permissions: []
    };
  const t = r.permissions || r["x-hasura-permissions"] || [];
  return {
    orgCode: r.org_code || r["x-hasura-org-code"],
    permissions: t
  };
}, Ie = async (e) => {
  var n;
  const r = await A("roles");
  if (!(r != null && r.value))
    return ((n = (await Y({
      url: "account_api/v1/roles"
    })).roles) == null ? void 0 : n.map((o) => ({
      id: o.id,
      name: o.name,
      key: o.key
    }))) || [];
  const t = await _();
  return t ? !t.roles && !t["x-hasura-roles"] ? (console.warn(
    "No roles found in token, ensure roles have been included in the token customisation within the application settings"
  ), []) : t.roles || t["x-hasura-roles"] : [];
}, $e = async () => {
  var r, t;
  const e = await Y({
    url: "account_api/v1/entitlements"
  });
  return {
    orgCode: e.org_code,
    plans: ((r = e.plans) == null ? void 0 : r.map((n) => ({
      key: n.key,
      subscribedOn: n.subscribed_on
    }))) || [],
    entitlements: ((t = e.entitlements) == null ? void 0 : t.map((n) => ({
      id: n.id,
      fixedCharge: n.fixed_charge,
      priceName: n.price_name,
      unitAmount: n.unit_amount,
      featureKey: n.feature_key,
      featureName: n.feature_name,
      entitlementLimitMax: n.entitlement_limit_max,
      entitlementLimitMin: n.entitlement_limit_min
    }))) || []
  };
};
function he(e, r) {
  if (!e)
    return null;
  const t = e.split(".");
  if (t.length !== 3)
    return null;
  const n = t[
    1
    /* body */
  ].replace(/-/g, "+").replace(/_/g, "/"), o = decodeURIComponent(
    atob(n).split("").map((s) => "%" + ("00" + s.charCodeAt(0).toString(16)).slice(-2)).join("")
  );
  return JSON.parse(o);
}
const _ = async (e = c.accessToken) => {
  const r = T();
  if (!r)
    return null;
  const t = await r.getSessionItem(
    e === "accessToken" ? c.accessToken : c.idToken
  );
  if (!t)
    return null;
  const n = he(t);
  return n || console.warn("No decoded token found"), n;
}, Q = async (e = "accessToken") => _(e), A = async (e, r = "accessToken") => {
  const t = await Q(r);
  return t ? {
    name: e,
    value: t[e]
  } : null;
}, Pe = async () => {
  const e = await _();
  return e ? e.org_code || e["x-hasura-org-code"] : null;
}, xe = async (e, r) => {
  var o;
  const t = await _();
  if (!t)
    return null;
  const n = t.feature_flags || t["x-hasura-feature-flags"];
  return n ? ((o = n[e]) == null ? void 0 : o.v) ?? null : null;
}, be = async () => {
  const e = await Q("idToken");
  if (!e)
    return null;
  const { sub: r } = e;
  return r ? {
    id: e.sub,
    givenName: e.given_name,
    familyName: e.family_name,
    email: e.email,
    picture: e.picture
  } : (console.error("No sub in idToken"), null);
}, Ee = async (e, r) => {
  const t = await _();
  if (!t)
    return {
      permissionKey: e,
      orgCode: null,
      isGranted: !1
    };
  const n = t.permissions || [];
  return {
    permissionKey: e,
    orgCode: t.org_code,
    isGranted: !!n.includes(e)
  };
}, Re = async () => {
  const e = await _("idToken");
  return e ? !e.org_codes && !e["x-hasura-org-codes"] ? (console.warn(
    "Org codes not found in token, ensure org codes have been included in the token customisation within the application settings"
  ), null) : e.org_codes || e["x-hasura-org-codes"] : null;
}, Ae = async (e) => {
  try {
    const r = await X({
      threshold: e == null ? void 0 : e.expiredThreshold
    });
    return r && (e != null && e.useRefreshToken) ? (await x({
      domain: e.domain,
      clientId: e.clientId
    })).success : !r;
  } catch (r) {
    return console.error("Error checking authentication:", r), !1;
  }
}, X = async (e) => {
  try {
    const r = await _("accessToken");
    return r ? r.exp ? r.exp < Math.floor(Date.now() / 1e3) + ((e == null ? void 0 : e.threshold) || 0) : (console.error("Token does not have an expiry"), !0) : !0;
  } catch (r) {
    return console.error("Error checking authentication:", r), !1;
  }
}, x = async ({
  domain: e,
  clientId: r,
  clientSecret: t,
  refreshType: n = y.refreshToken,
  onRefresh: o
}) => {
  var h;
  const s = (f) => (o && o(f), f);
  if (!e)
    return s({
      success: !1,
      error: "Domain is required for token refresh"
    });
  if (!r)
    return s({
      success: !1,
      error: "Client ID is required for token refresh"
    });
  let l = "", d;
  if (i.useInsecureForRefreshToken || !E(e) ? d = $() : d = T(), n === y.refreshToken) {
    if (!d)
      return s({
        success: !1,
        error: "No active storage found"
      });
    if (l = await d.getSessionItem(
      c.refreshToken
    ), !l)
      return s({
        success: !1,
        error: "No refresh token found"
      });
  }
  O();
  let u, a;
  try {
    if (i.onRefreshHandler)
      u = await i.onRefreshHandler(n);
    else {
      const f = new URLSearchParams({
        ...n === y.refreshToken && {
          refresh_token: l
        },
        grant_type: "refresh_token",
        client_id: r
      });
      t && f.append("client_secret", t);
      const m = await fetch(`${j(e)}/oauth2/token`, {
        method: "POST",
        ...n === y.cookie && { credentials: "include" },
        headers: {
          "Content-type": "application/x-www-form-urlencoded; charset=UTF-8"
        },
        body: f.toString()
      });
      if (!m.ok)
        return s({
          success: !1,
          error: "Failed to refresh token"
        });
      a = await m.json(), u = {
        success: !0,
        [c.accessToken]: a.access_token,
        [c.idToken]: a.id_token,
        [c.refreshToken]: a.refresh_token
      };
    }
    if (u.accessToken) {
      const f = T();
      if (!f)
        return s({
          success: !1,
          error: "No active storage found"
        });
      if (await f.setSessionItem(
        c.accessToken,
        u.accessToken
      ), u.idToken && await f.setSessionItem(c.idToken, u.idToken), d && u.refreshToken && await d.setSessionItem(
        c.refreshToken,
        u.refreshToken
      ), R()) {
        const m = Number((h = await A("exp")) == null ? void 0 : h.value);
        if (Number.isFinite(m) || a != null && a.expires_in) {
          let S = 0;
          if (!(a != null && a.expires_in)) {
            const v = Math.floor(Date.now() / 1e3);
            S = Math.max(m - v, 1);
          }
          q((a == null ? void 0 : a.expires_in) || S, async () => {
            x({ domain: e, clientId: r, refreshType: n, onRefresh: o });
          });
        }
      }
      return s(u);
    }
  } catch (f) {
    return s({
      success: !1,
      error: `No access token received: ${f}`
    });
  }
  return s({
    success: !1,
    error: "No access token received"
  });
}, k = {
  secure: null,
  insecure: null
}, Ce = (e) => {
  if (i.activityTimeoutMinutes) {
    k.secure = J(e);
    return;
  }
  k.secure = e;
}, T = () => k.secure || null, Ue = () => {
  k.secure = null;
}, Le = (e) => {
  if (i.activityTimeoutMinutes) {
    k.insecure = J(e);
    return;
  }
  k.insecure = e;
}, $ = () => k.insecure || k.secure || null;
function me(e, r = []) {
  try {
    const t = new URL(e);
    return !r.includes(t.protocol) && !!t.host;
  } catch {
    return !1;
  }
}
const Me = async ({
  domain: e,
  returnUrl: r,
  subNav: t
}) => {
  const n = T();
  if (!n)
    throw new Error("generatePortalUrl: Active storage not found");
  const o = await n.getSessionItem(
    c.accessToken
  );
  if (!o)
    throw new Error("generatePortalUrl: Access Token not found");
  if (!e || typeof e != "string") {
    const u = await A("iss");
    if (!(u != null && u.value) || typeof u.value != "string")
      throw new Error(
        "generatePortalUrl: Unable to determine domain from access token"
      );
    e = u.value;
  }
  if (!me(r, ["ftp:", "ws:"]))
    throw new Error("generatePortalUrl: returnUrl must be an absolute URL");
  const s = new URLSearchParams({
    subnav: t || H.profile,
    return_url: r
  }), l = await fetch(
    `${j(e)}/account_api/v1/portal_link?${s.toString()}`,
    {
      headers: {
        Authorization: `Bearer ${o}`
      }
    }
  );
  if (!l.ok)
    throw new Error(
      `Failed to fetch profile URL: ${l.status} ${l.statusText}`
    );
  const d = await l.json();
  if (!d.url || typeof d.url != "string")
    throw new Error("Invalid URL received from API");
  try {
    return {
      url: new URL(d.url)
    };
  } catch (u) {
    throw console.error(u), new Error(`Invalid URL format received from API: ${d.url}`);
  }
}, ge = () => window.self !== window.top, Ne = async ({
  url: e,
  popupOptions: r = {},
  handleResult: t,
  forcePopup: n = !1
}) => {
  ge() || n ? await pe({
    url: e,
    popupOptions: r,
    handleResult: t
  }) : document.location = e;
}, pe = async ({
  url: e,
  popupOptions: r = {},
  handleResult: t
}) => {
  const {
    width: n = 500,
    height: o = 600,
    left: s = (window.screen.width - n) / 2,
    top: l = (window.screen.height - o) / 2
  } = r, d = window.open(
    e,
    "kinde_auth_popup",
    `width=${n},height=${o},left=${s},top=${l},scrollbars=yes,resizable=yes,toolbar=no,menubar=no,location=no,status=no`
  );
  if (!d)
    throw new Error("Popup was blocked by the browser");
  const u = () => new Promise((a) => {
    const h = (f) => {
      if (f.origin === window.location.origin && f.data && f.data.type === "KINDE_AUTH_RESULT") {
        window.removeEventListener("message", h);
        const m = new URLSearchParams();
        Object.entries(f.data.result).forEach(([S, v]) => {
          m.append(S, v);
        }), t == null || t(m).then(() => a());
      }
    };
    window.addEventListener("message", h);
  });
  try {
    await u();
  } catch (a) {
    throw new Error("Popup authentication failed: " + a);
  }
  return d;
};
export {
  L as IssuerRouteTypes,
  Se as LocalStorage,
  _e as MemoryStorage,
  H as PortalPage,
  K as PromptTypes,
  y as RefreshType,
  G as SessionBase,
  c as StorageKeys,
  F as base64UrlEncode,
  Te as checkAuth,
  Ue as clearActiveStorage,
  O as clearRefreshTimer,
  ke as exchangeAuthCode,
  I as frameworkSettings,
  ye as generateAuthUrl,
  le as generateKindeSDKHeader,
  Me as generatePortalUrl,
  M as generateRandomString,
  T as getActiveStorage,
  A as getClaim,
  Q as getClaims,
  Pe as getCurrentOrganization,
  _ as getDecodedToken,
  $e as getEntitlements,
  xe as getFlag,
  $ as getInsecureStorage,
  Ee as getPermission,
  ve as getPermissions,
  Ie as getRoles,
  Re as getUserOrganizations,
  be as getUserProfile,
  Ae as isAuthenticated,
  R as isClient,
  E as isCustomDomain,
  X as isTokenExpired,
  ie as mapLoginMethodParamsForUrl,
  Ne as navigateToKinde,
  x as refreshToken,
  j as sanitizeUrl,
  J as sessionManagerActivityProxy,
  Ce as setActiveStorage,
  Le as setInsecureStorage,
  q as setRefreshTimer,
  B as splitString,
  i as storageSettings,
  fe as updateActivityTimestamp
};
